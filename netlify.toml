[build]
  base    = "frontend"
  command = "yarn build"
  publish = "build"

[build.environment]
  NODE_VERSION          = "20"
  CI                    = "false"
  DISABLE_ESLINT_PLUGIN = "true"
  SKIP_PREFLIGHT_CHECK  = "true"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
